class Stack :
    def __init__(self, element):
        self._element = list
    def Stack(self):
       stack = []
       return stack
    def isEmpty(self, list):
        length = len(list)
        if length == 0:
            return True
        else:
            False

    def peek(self, list):
        list = list.peek()
        return list
    def push(self, list):
        list = list.push()
        return list
    def pop(self, list):
        list = list.pop()
        return list
    def getSize(self,list):
        length = len(list)
        return length